import React from 'react';

const Footer = ({ className }) => {
    return (
        <div className={className}>
           <h2>Los mensajes de whatsapp están deshabilitados temporalmente</h2>
        </div>
    );
};

export default Footer;